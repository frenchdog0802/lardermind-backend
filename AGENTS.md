# Agent instructions (LarderMind)

**Hard rules:**

1. **Adding a new feature MUST use the new feature flow.**
2. **Fixing a bug MUST use the bug fix flow.**
3. **If you encounter a problem → ask the user first.** Do not guess.
4. **Otherwise → finish the task.** Do not stop early on clear work.

Source of truth: [`docs/workflow/README.md`](docs/workflow/README.md)  
Templates: `docs/workflow/templates/`  
Cursor rule: `.cursor/rules/dev-workflow.mdc` (always apply)

Do not treat `d:\dev\AI workflow\` as authoritative if it conflicts with `docs/workflow/`.
