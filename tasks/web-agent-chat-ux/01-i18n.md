# 01 — i18n (en + zh)

## Goal

Add / update copy for agent empty state, chips, suggestion cards, and composer placeholder.

## Acceptance

- [ ] `ai.greetingHi` with `{{name}}` (en + zh)
- [ ] `ai.chipPantry` / `ai.chipBuy` with `{{count}}`
- [ ] `ai.suggestedPromptCards` as `{ title, prompt }[]` (4 cards; titles short; prompts actionable)
- [ ] `ai.placeholder` product-specific (cooking / pantry / planning)
- [ ] Keep `ai.suggestedPrompts` if DemoChatBox still depends on it
- [ ] Matches locked strings in design §3.1
