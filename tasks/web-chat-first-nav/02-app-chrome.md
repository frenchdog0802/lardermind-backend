# 02 — AppHeader + AppDrawer

## Goal

Add shared chrome components per design §3.2.

## Acceptance

- [ ] `AppHeader.tsx`: hamburger (`onOpenMenu`), optional New chat, optional back
- [ ] `AppDrawer.tsx`: overlay, primary (Chat/Calendar/Inventory/Shopping/Recipes), Recents via `listSessions`, footer New chat / Settings / Subscription
- [ ] Esc + backdrop close; body scroll lock handled by App or drawer
- [ ] Warm Kitchen tokens only
