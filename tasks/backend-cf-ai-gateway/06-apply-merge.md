# Task 06: pantry-vision apply + merge

`POST /api/pantry-vision/apply` + `mergeAddPantryItem` by case-insensitive name.

## Acceptance

- [ ] Merge vs add counts correct
- [ ] No quota increment on apply
