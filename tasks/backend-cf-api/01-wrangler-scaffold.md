# Task 01: Wrangler scaffold

**Phase:** 1 — Scaffold  
**Depends on:** None  
**Blocks:** 02–11

## Description

Create `backend-cf/` with `package.json`, `wrangler.toml`, `tsconfig.json`, Hono + wrangler devDependencies.

## Files

| Action | Path |
|--------|------|
| Create | `backend-cf/package.json` |
| Create | `backend-cf/wrangler.toml` |
| Create | `backend-cf/tsconfig.json` |

## Acceptance criteria

- [ ] `npm install` succeeds in `backend-cf/`
- [ ] `wrangler.toml` binds `DB`, `AI`, documents D1 placeholder id
