# Task 03: Frontend README + wrangler comments

**Repo:** `frontend/` (lardermind-frontend)

## Goal

Document Pages + Workers as the production path; remove Nest as the primary API story.

## Work

1. Rewrite `README.md` Local / Pages sections for `backend-cf` `:8787` and Workers `VITE_API_BASE_URL`.
2. Document GitHub secrets/vars for the Pages workflow.
3. Update `wrangler.toml` comments (Workers URL, CORS on Worker — not Nest).

## Acceptance

- [ ] README matches design operator setup
- [ ] `wrangler.toml` comments reference Workers / Pages CORS
