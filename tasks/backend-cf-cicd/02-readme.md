# 02 — Document CI secrets

## Goal

Update `backend-cf/README.md` with CI/CD section: required GitHub secrets and what the workflow does.

## Acceptance

- [ ] Operator can set secrets without reading the workflow YAML
